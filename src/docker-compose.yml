version: '3.7'

services:
  api:
    image: eiximenis/s2020-api
    build:
      context: api
      dockerfile: Dockerfile
  web:
    image: eiximenis/s2020-web
    build:
      context: BeersWeb
      dockerfile: Dockerfile